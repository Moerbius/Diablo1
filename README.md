# Diablo1

SDL3 Diablo1 implementation

WORK IN PROGRESS

## Build

### Make

```bash
make
./diablo1
```

### CMake

```bash
cmake -S . -B build-cmake
cmake --build build-cmake
./build-cmake/diablo1
```

Both build paths expect SDL3 development files to be installed on the system.
